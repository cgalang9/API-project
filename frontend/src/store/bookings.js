import { csrfFetch } from "./csrf"



const initialState = {}

export const bookingsReducer = (state = initialState, action) => {
    switch(action.type) {
        default:
            return state
    }

}
